buildscript {
    repositories {
        jcenter()
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    configurations {
        provided
        compile.extendsFrom provided
    }
    tasks.withType(JavaCompile) {
        sourceCompatibility = '1.6'
    }
}
